
RaspBot Tests
=============

There are now too many tests to compile them all in a sensible time,
and you probably don't want to compile or run them all anyway, so they
have been separated out.

To compile an individual test, just type

    make testName

To really compile everything:

    make really-all

The individual tests are:

